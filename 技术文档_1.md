# 单腿仿真-真机联动流程说明

这份文档解释当前这套程序是怎么工作的。当前目标不是完整四足机器人 RL policy 上真机，而是先把 IsaacSim 里的单腿手动运动，同步到真实单腿电机上，用来验证方向、零位、角度比例和安全范围。

## 1. 当前系统由哪些部分组成

当前有两个主要程序：

1. `D:\IsaacLab\source\isaaclab_tasks\isaaclab_tasks\manager_based\locomotion\velocity\config\bennett_test4\bennett_single_leg_ik_trace.py`

   这是 IsaacSim / IsaacLab 里的单腿仿真程序。现在默认是 `manual` 模式，也就是用键盘控制仿真里的 FR 腿。

2. `E:\HuanCun\Desktop\u2canfd\damiao.py`

   这是真实电机控制程序。它通过达妙 USB-CANFD 模块控制真实电机。现在它监听 IsaacSim 发来的 UDP 数据，并控制真实 RR 腿的 `canid7` / `canid8`，临时把真实 RR 腿当成仿真 FR 腿来验证。

数据流是：

```text
键盘
  -> IsaacSim 里的 FR_thigh / FR_calf 目标角
  -> UDP 发给 damiao.py
  -> damiao.py 换算成 canid7 / canid8 的 MIT 位置命令
  -> USB-CANFD
  -> 达妙电机
  -> 真实单腿跟着动
```

## 2. UDP 是什么


UDP 可以理解成一种很轻量的本机数据发送方式。它不需要建立连接，也不会像 TCP 那样确认每一包一定送到。优点是简单、延迟低，适合实时控制里传小数据。

这里 UDP 没有经过互联网，也不是远程联网。当前只在本机发送：

```text
127.0.0.1:15001
```

`127.0.0.1` 表示本机，`15001` 是端口号。

当前实现方式是：

1. IsaacSim 脚本作为发送端。

   在 `manual` 模式下，它会周期性发送一段 JSON 数据：

   ```json
   {
     "leg": "FR",
     "thigh_offset_rad": 0.123,
     "calf_offset_rad": -0.045,
     "time_s": 1.23
   }
   ```

   这里发送的是“相对默认站姿的关节偏移量”，单位是 rad，不是电机 CAN 命令。

2. `damiao.py` 作为接收端。

   它监听：

   ```python
   udp_sock.bind(("127.0.0.1", 15001))
   ```

   然后用：

   ```python
   packet, _addr = udp_sock.recvfrom(4096)
   ```

   收到 IsaacSim 发来的 `thigh_offset_rad` 和 `calf_offset_rad`。

使用 UDP 的原因是：IsaacSim 和电机控制程序是两个独立进程，一个负责仿真画面和键盘输入，一个负责真实电机控制。UDP 让它们之间可以实时传目标角。

## 3. manual 模式是什么

`manual` 模式就是“手动控制模式”。它不是强化学习 policy，也不是自动步态。

在 `bennett_single_leg_ik_trace.py` 里：

```python
DEFAULT_MODE = "manual"
```

表示默认启动后就是手动模式。

键盘控制逻辑是：

```text
Up / Down      控制 thigh 目标角
Left / Right   控制 calf 目标角
R              目标角回到默认站姿
C              清除仿真里的足端轨迹
Q / Esc         退出
```

manual 模式的用途：

1. 验证仿真关节方向和真实电机方向是否一致。
2. 验证仿真默认站姿和真实机械默认姿态是否对应。
3. 验证仿真 rad 和电机 MIT q 的换算比例。
4. 在上 RL policy 之前，先用人手控制，降低调试风险。

也就是说，manual 模式是 sim2real 前的低风险联调工具。它帮你确认“仿真发出的目标角，真实电机是不是按预期动”。

## 4. 减速比 6:1 怎么理解

你当前电机减速比是 6:1。简单理解：

```text
关节输出端转 1 圈，电机内部大约转 6 圈。
```

如果 MIT 模式里的 `q` 用的是电机侧角度，那么关节侧和电机侧的角度关系就是：

```text
电机角度变化 = 关节角度变化 * 6
```

所以：

```text
关节 +1 deg  -> 电机 +6 deg
关节 +10 deg -> 电机 +60 deg
关节 +20 deg -> 电机 +120 deg
```

用 rad 表示：

```text
20 deg = 0.349 rad
电机侧变化 = 0.349 * 6 = 2.094 rad
```

当前 `damiao.py` 的核心换算就是：

```python
q_motor = q_motor_default + sign * 6.0 * q_joint_offset
```

其中：

- `q_motor_default`：真实电机默认站姿时读到的位置。
- `sign`：方向符号，方向反了就改 `+` / `-`。
- `6.0`：减速比。
- `q_joint_offset`：IsaacSim 发来的关节偏移，单位 rad。

## 5. 当前真实电机映射

当前真实安装的是 RR 腿，但为了先验证单腿流程，暂时把它当成仿真里的 FR 腿。

当前映射是：

```text
仿真 FR_thigh -> 真实 RR thigh -> canid7
仿真 FR_calf  -> 真实 RR calf  -> canid8
```

当前默认电机位置是实测记录值：

```python
m7_default = 0.089
m8_default = 1.433
```

当前换算关系是：

```python
q7_cmd = m7_default + 6.0 * fr_thigh_offset
q8_cmd = m8_default - 6.0 * fr_calf_offset
```

也就是：

```text
canid7: 和仿真 FR_thigh 偏移同号
canid8: 和仿真 FR_calf 偏移反号
```

注意：这个方向是根据你现场观察修正过的，不是随便猜的。以后如果换腿、换安装方向、换连杆结构，需要重新验证方向。

## 6. MIT 模式在这里做什么

达妙 MIT 控制命令形式是：

```python
control.control_mit(motor, kp, kd, q, dq, tau)
```

当前 `damiao.py` 发的是：

```python
control.control_mit(motor7, 35.0, 1.5, q7_cmd, 0.0, 0.0)
control.control_mit(motor8, 35.0, 1.5, q8_cmd, 0.0, 0.0)
```

含义是：

```text
kp  = 35.0   位置刚度，越大越想追到 q
kd  = 1.5    阻尼，抑制震荡和间断运动
q   = 目标位置
dq  = 0.0    目标速度为 0
tau = 0.0    不额外加前馈力矩
```

所以当前不是速度模式，也不是直接给电机固定转速，而是“给目标位置，让电机用 MIT 位置控制去跟踪”。

## 7. 运行顺序

先启动真实电机接收端，再启动 IsaacSim 发送端。

### 第一步：启动达妙电机程序

在 PowerShell 中：

```powershell
cd E:\HuanCun\Desktop\u2canfd
python damiao.py
```

看到类似输出说明它在等待 IsaacSim 数据：

```text
**********Motor_Control init success**********
[UDP] listening on 127.0.0.1:15001; IsaacSim FR -> real RR canid7/canid8
```

### 第二步：启动 IsaacSim 单腿程序

在另一个终端中：

```powershell
cd D:\IsaacLab
.\isaaclab.bat -p source\isaaclab_tasks\isaaclab_tasks\manager_based\locomotion\velocity\config\bennett_test4\bennett_single_leg_ik_trace.py --leg FR --mode manual
```

启动后，在 IsaacSim 窗口里用方向键控制仿真 FR 腿。真实 RR 腿会按当前映射跟随。

### 第三步：看 `damiao.py` 输出

输出类似：

```text
thigh_deg:+1.2 calf_deg:-0.8 udp_age:0.02s cmd7:0.215 cmd8:1.517 m7 pos:0.210 vel:0.011 tau:0.063 err:1 | m8 pos:1.520 vel:-0.011 tau:-0.171 err:1 |
```

重点看：

```text
thigh_deg / calf_deg  IsaacSim 发来的关节偏移，单位度
cmd7 / cmd8           算出来的电机目标位置
m7 pos / m8 pos       电机实际反馈位置
tau                   电机反馈力矩
err                   电机状态
udp_age               距离上一包 UDP 的时间
```

如果 `cmd7/cmd8` 在变，但 `m7 pos/m8 pos` 不跟着变，就说明电机没有正确跟踪。可能是使能、CAN、负载、kp/kd、方向或安全保护问题。

## 8. 当前安全保护

当前 `damiao.py` 做了几个基础安全限制：

1. 角度限制：

   ```python
   fr_limit = 20.0 * pi / 180.0
   ```

   即真实硬件只允许跟随 IsaacSim 发来的正负 20 度关节偏移。

2. UDP 超时：

   如果一段时间没收到新 UDP 包，当前代码保持最后目标，不继续变化。

3. 电机错误状态：

   如果 `err` 不是正常状态，会停掉所有电机。

4. 力矩限制：

   当前设置：

   ```python
   udp_tau_limit = 2.0
   ```

   如果启动前 100 个控制周期之后，反馈力矩绝对值超过 2.0，就停掉所有电机。

注意：这些只是基础保护，不等于完整机器人安全系统。后续上 RL policy 前，还需要加入更完整的限位、急停、上电 hold 当前角度、缓慢 ramp 到默认站姿、通信丢失处理、温度/电压/电流保护。

## 9. 当前做到哪一步了

目前已经做到：

```text
IsaacSim manual 单腿目标角
  -> UDP
  -> damiao.py
  -> canid7/canid8 MIT 位置控制
  -> 真实 RR 单腿跟随仿真 FR 单腿
```

这可以叫：

```text
单腿仿真-真机联动
单腿硬件跟随仿真
单腿 sim2real 联调
```

但还不能叫完整的：

```text
RL policy sim2real
```

因为现在控制目标来自键盘，不来自训练好的神经网络 policy。

## 10. 和后续 RL policy sim2real 的关系

后续真正的 RL policy sim2real 大概会变成：

```text
真实传感器反馈
  -> 组装成和训练时一致的 observation
  -> policy 推理
  -> 输出 action
  -> action 转成关节目标角
  -> 经过方向、零位、减速比、安全限幅
  -> MIT 命令发给多个电机
```

你现在做的这一步，是其中最底层、最关键的一段：

```text
仿真关节目标角 -> 真实电机目标角
```

这一步不通，RL policy 上来一定会乱动。现在先用 manual 模式验证，是正确顺序。

## 11. 下一步建议

下一步不要直接上完整 RL policy，建议按下面顺序做：

1. 固化 canid7/canid8 的方向、默认位置和安全角度范围。
2. 把 `damiao.py` 里当前这套映射整理成清晰配置表，不要散在循环里。
3. 给 UDP 数据加入更严格的超时策略：超过 0.5 秒没有 IsaacSim 数据，就 hold 当前实际位置或缓慢回默认姿态。
4. 把单腿从 manual 控制升级到 IsaacSim 自动轨迹，验证重复运动稳定性。
5. 再扩展到 8 个电机。
6. 最后才接 RL policy 输出。

## 12. 常见误解

### UDP 是不是控制电机的协议？

不是。UDP 只是 IsaacSim 和 `damiao.py` 之间传目标角的方式。真正控制电机的是 `damiao.py` 通过 USB-CANFD 发出的达妙 MIT 命令。

### manual 模式是不是 RL？

不是。manual 模式是人按键盘控制目标角。RL policy 是神经网络根据 observation 自动输出 action。

### 20 度限制是电机限位吗？

不是电机内部硬限位。它是当前程序人为加的软件限幅，用来降低调试风险。

### 减速比 6:1 是不是所有方向都一样？

比例大小一样，但方向不一定一样。方向由机械安装决定，所以才有 `sign`。当前 canid7 和 canid8 的方向是现场验证出来的。

### 现在能不能直接 sim2real 跑强化学习？

还不能直接跑完整 RL。现在只是把“仿真单腿目标角同步到真实单腿”这条链路打通了。完整 RL 还需要 observation、policy、动作缩放、所有关节映射、安全系统和实机状态反馈。
