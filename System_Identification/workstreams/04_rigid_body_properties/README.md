# 04 — 核对尺寸、质量、质心和惯量

这里负责机器人“骨架和重量”的参数，不和电机的有效折算惯量混在一起。

优先顺序：

1. 从 CAD 和零件清单拿到初值；
2. 用实物称重检查；
3. 用支撑或悬挂方法检查质心；
4. 只有确有需要时，才做摆动试验估计惯量。

每个数字都要写清坐标方向、单位、包含哪些零件以及对应哪个机器人版本。

完成后应得到参数清单、CAD 与实测差异和修改建议。没有验证前，不直接覆盖训练正在使用的机器人模型。
