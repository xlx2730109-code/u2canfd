# 06 — 建立统一的数据记录工具

这里负责“怎么把每次实验完整、可靠地记下来”，不负责计算摩擦和惯量。

要统一记录命令、真实反馈、IMU、电源、错误和人工操作事件。每次运行还要生成一份实验身份证，写清硬件、代码、参数和安全条件。

后续优先利用父目录现有的 `quad_leg_xu/bennett_deploy/async_csv.py`，不重新造一套互不兼容的记录系统。正式源代码仍留在父目录，这里保存本工作需要的配置、测试和结果。

第一项验收完全不动真机：用假数据生成一次完整实验，随后处理数据，并且能发现丢帧、重复读取旧缓存和时间戳异常。
