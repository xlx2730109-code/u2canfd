# 03 — 对关节顺序、方向和零位

这里负责确认“仿真里的这个关节，到底对应哪台真实电机，正方向是不是同一个方向”。

每个关节都要记录：仿真名、真实机械位置、CAN ID、排列序号、正负方向、零位、软件偏移、比例和机械限位。

如果软件给命令和读反馈时同时乘了同一个负号，画出来仍然可能看似正确，但实体方向其实是错的。因此必须有人观察真实运动，或使用独立角度测量，不能只靠代码自己证明自己。

完成后应得到 `joint_map.yaml`、每个关节的实物验证证据、零位结果、机械限位和重复测量结果。
