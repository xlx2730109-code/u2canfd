# 10 — 用新动作检验模型

这里相当于考试。它会把同一条真实命令放进不同仿真模型，比较：原始模型、达妙官方曲线候选、系统辨识后的模型，以及确有需要的复杂模型。

用来考试的数据不能参与调参。至少要看每个关节的整体误差、最大误差、动作晚多少、不同频率下能否跟上、重复试验是否稳定，以及多次自动搜索得到的参数是否接近。

最后只给三种结论：通过、不通过、还需要补什么实验。即使通过，也不会自动修改正式训练配置。
